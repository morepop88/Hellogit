<%= form_with(model: schedule_history) do |form| %>
  <% if schedule_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule_history.errors.count, "error") %> prohibited this schedule_history from being saved:</h2>

      <ul>
        <% schedule_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_name %>
    <%= form.text_field :task_name %>
  </div>

  <div class="field">
    <%= form.label :start_at %>
    <%= form.datetime_select :start_at %>
  </div>

  <div class="field">
    <%= form.label :end_at %>
    <%= form.datetime_select :end_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
